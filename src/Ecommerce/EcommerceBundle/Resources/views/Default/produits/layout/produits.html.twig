{% extends "layout/layout.html.twig" %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="span3">
            {% include 'moduleUsed/navigation.html.twig' %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    {% include '@Users/Default/modulesUsed/utilisateursConnectes.html.twig' %}
                {% else %}
                    {% include '@Users/Default/modulesUsed/utilisateurs.html.twig' %}
                {% endif %}
                {{ render(controller("EcommerceBundle:panier:menu")) }}
            </div>
            <div id="productList" class="span9">
                {{ include('@Ecommerce/Default/produits/layout/produits-list.html.twig') }}
                <div class="navigation">
                    {{ knp_pagination_render(produits) }}
                </div>
            </div>

        </div>

    </div>

{% endblock %}
